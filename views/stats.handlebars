<div class="container">
    <div class="card stats-card">
        <h2 class="card-title text-center">URL Stats</h2>
        <p>Full URL: <a href="{{urlStats.long}}">{{urlStats.long}}</a></p>
        <p>Shortcode: {{urlStats.short}}</p>
        <p>Number of Times Accessed: {{urlStats.count}}</p>
        <p>Registered: {{urlStats.createdAt}}</p>
        <p>Last Accessed: {{urlStats.updatedAt}}</p>
    </div>
</div>